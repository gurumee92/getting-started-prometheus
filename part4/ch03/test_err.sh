while :
do
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  (curl localhost:8080/) &
  wait
  sleep 1
done